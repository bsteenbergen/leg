task isPalindrome(str toCheck) yields bool palindrome:
    num left = 0
    num right = toCheck.length - 1

    loop until left >= right:
        if toCheck[left] != toCheck[right] palindrome = false
        left++
        right++
        
    palindrome = true



    