mum {
    Program = Stmt+
    
    Stmt = print (float | int | str | id)		  --print
    	| Type id "=" (float | int | str | id)     --vardecl
        
    Type = "int" | "float" | "str"
    
    id = letter alnum*

    str = "\"" char* "\""
    int = ("-")? digit+
    float = ("-")? digit+ "." digit+ (("E" | "e") ("+" | "-")? digit+)?

    print = "print" ~alnum
        
    char = ~"\"" ~"\n" any 
    space += "@" (~"\n" any)* --comment 
}